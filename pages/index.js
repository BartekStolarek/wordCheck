import Layout from '../components/Layout';
import Input from '../components/Input';

const Index = (props) => (
    <Layout>
        <div>
            <h1>Welcome to WordCheck</h1>
        </div>
        <Input />
    </Layout>
);

export default Index;